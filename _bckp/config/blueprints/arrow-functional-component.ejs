import React, { PropTypes } from 'react';

let <%= helpers.capitalize(name) %> = (props, context) => {
  return (
  );
};

<%= helpers.capitalize(name) %>.propTypes = {
};

export default <%= helpers.capitalize(name) %>;
